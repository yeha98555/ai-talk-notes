# Copy this file to .env.local and fill in your real key. NEVER commit the real key.
#   cp .env.example .env.local
#   node --env-file=.env.local tools/gen-note.mjs <videoId>
#
# .env.local is git-ignored; this .example template (placeholder only) is tracked.

# Anthropic API key used by tools/gen-note.mjs (Claude Messages API).
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
